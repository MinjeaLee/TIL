// Create a program that only receives a string that does not contain spaces from the user and then prints only the lowercase letter.

// Input Example 1: HelloWorld
// Output Example 1: elloorld
// Input Example 2: AbCdEEff
// Output Example 2: bdff

#pragma warning(disable:4996)
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(){
    char str[100];
    printf("Enter a string: ");
    scanf("%s", str);
    int i = 0;
    

    return 0;
}