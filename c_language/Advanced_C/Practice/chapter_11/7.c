#include <stdio.h>

struct chart{
    int score, rangking;
};

int main(){
    struct chart st[10];
    int index = 0, score;

    for(int i = 0; i < 10; i++, index++){
        scanf("%d", &st[i].score);
        for(int j = 0; j < index; j++){

        }
    }

    return 0;
}